# 청라강성교회 홈페이지 프로젝트

블로그·유튜브와 자동 연동되는 교회 홈페이지 샘플 모음.
`[대괄호]`로 표시된 부분만 나중에 내용을 채우면 됩니다.

## 폴더 구성

```
C:\Homepage\
├── blog\                  ← 1단계: 네이버 블로그 개편 샘플
│   ├── 01-카테고리-개편안.md
│   ├── 02-고정글-새가족안내.md          (공지 고정용)
│   ├── 03-고정글-교회소개-인사말.md
│   ├── 04-고정글-예배시간안내.md
│   ├── 05-고정글-오시는길.md
│   ├── 06-매주글-주일예배-템플릿.md     (매주 복사해서 사용)
│   ├── 07-매주글-주보-템플릿.md         (매주 복사해서 사용)
│   └── 08-홈페이지형-배너-위젯-가이드.md (세움교회 방식)
└── site\                  ← 3단계: 자동 연동 홈페이지 샘플
    ├── 미리보기.bat        ★ 더블클릭하면 브라우저로 미리보기 (이걸로 여세요!)
    ├── index.html         (메인 페이지 — 유튜브 자동 연동 작동함)
    ├── style.css
    ├── script.js          (블로그 최신 글 표시)
    ├── data\posts.json    (샘플 데이터 — 자동화 전 임시)
    ├── scripts\serve.ps1                (미리보기 서버)
    ├── scripts\fetch-feeds.mjs          (RSS → posts.json 생성기)
    └── .github\workflows\update-feeds.yml (매일 자동 갱신)
```

## ⚠️ 홈페이지 미리보기 방법

`index.html`을 **직접 더블클릭하면 안 됩니다** — 브라우저 보안 정책(file://) 때문에
유튜브 영상과 소식 카드가 차단되어 빈 화면처럼 보입니다.

대신 **`site\미리보기.bat`을 더블클릭**하세요.
검은 창과 함께 브라우저가 자동으로 열리고 모든 것이 정상 표시됩니다.
(검은 창을 닫으면 미리보기가 종료됩니다. 인터넷에 배포한 뒤에는 이런 과정 없이 누구나 바로 봅니다.)

## 진행 순서 체크리스트

### 1단계 — 블로그 개편 (이번 주)
- [ ] `01-카테고리-개편안.md`대로 카테고리 신설·이동·비공개 처리
- [ ] 고정 글 4개(02~05) 내용 채워서 게시
- [ ] 새가족안내 글을 **공지로 고정** (모바일 첫 화면 대응)
- [ ] 블로그 소개글에 예배시간 한 줄 기입

### 2단계 — 홈페이지형 블로그 꾸미기
- [ ] 배너 이미지 제작 (08번 가이드의 규격 참고, 캔바)
- [ ] 타이틀 등록 + 투명 위젯 5개 등록 (08번 가이드의 코드 사용)
- [ ] 프롤로그형 메인 설정

### 3단계 — 자동 연동 홈페이지 (블로그가 2~3주 돌아간 뒤)
- [ ] GitHub 계정 만들기 → `site\` 내용으로 저장소 생성
- [ ] GitHub Pages 켜기 (Settings → Pages)
- [ ] Actions 자동 갱신 확인 (매일 새벽 블로그 RSS 수집)
- [ ] (선택) 도메인 구입·연결

## 행사 고정 배너 관리법 (여름성경학교 등)

홈페이지 메인 상단에 고정 배너가 표시됩니다. **`site\data\notice.json` 파일 하나만 수정**하면 됩니다.
여러 개를 동시에 띄울 수 있습니다 — 목록에 `{ }` 항목을 추가하면 되고, 2개 이상이면 나란히 배치됩니다.
(현재: ① 개척이야기(상시) + ② 여름성경학교(6/1~8/15) 2개)

각 배너 항목:

| 항목 | 설명 |
|---|---|
| `active` | `true`=표시 / `false`=숨김 |
| `badge` | 노란 배지 글자 (예: "여름성경학교") |
| `title` / `description` | 제목과 한 줄 설명 |
| `link` | 누르면 이동할 곳 (보통 블로그 행사 글 주소) |
| `image` | 포스터 이미지 주소 (블로그 글의 카드 썸네일 주소를 그대로 써도 됨) |
| `start` / `end` | 표시 기간 — **end가 지나면 자동으로 사라짐** (내릴 걱정 불필요) |

새 행사 때는 이 파일의 내용만 갈아끼우면 끝. 행사 글을 먼저 블로그에 쓰고, 그 글 주소를 `link`에 넣는 방식을 권장합니다.

## 이웃사랑 섹션 관리법

홈페이지 하단에 이웃사랑(나눔·구제) 이야기가 고정 표시됩니다. **`site\data\outreach.json`**에서 관리:
- 현재: 성탄헌금 장애자녀가정 지원(2026-02) + 굿윌스토어 물품기증(2025-05)
- 새 나눔 이야기가 생기면: 블로그에 글 작성 → outreach.json에 항목 추가(제목/한줄소개/링크/날짜/이미지)
- "더 보기" 버튼은 블로그 이웃사랑 카테고리로 연결됨

## 매주 운영 루틴 (회당 15~20분)

| 요일 | 할 일 | 사용할 템플릿 |
|---|---|---|
| 주일 오후 | 주일예배 글 1편 (유튜브 임베드+요약) | `06-매주글-주일예배-템플릿.md` |
| 주일 오후 | 주보 글 1편 | `07-매주글-주보-템플릿.md` |
| 발행 주기에 맞춰 | 오렌지카드(가정예배) 글 | `09-매주글-오렌지카드-템플릿.md` |
| 발행 주기에 맞춰 | 선포기도문(밤기도회) 글 | `10-매주글-선포기도문-템플릿.md` |

> 오렌지카드·선포기도문은 **제목에 "[오렌지카드]" / "[선포기도문]"만 들어가면**
> 홈페이지 '가정예배 · 밤기도회' 코너에 최신 글이 자동으로 표시됩니다. 다만 해당 주의 가정예배 대시보드가 등록되어 있으면 오렌지카드 칸은 대시보드를 우선 보여 줍니다.
| (유튜브·홈페이지) | **할 일 없음 — 자동 반영** | |

### 오렌지카드 가족 대시보드

새 오렌지카드 이미지를 Codex에 보내고 “가정예배 대시보드로 만들어줘”라고 요청해 주세요. 카드 내용을 확인해 가족용 화면을 만든 뒤, `site/data/orange-dashboards.json`에 날짜와 표시 기간을 등록하면 홈페이지의 오렌지카드 칸이 해당 주 화면으로 연결됩니다. 기간이 끝나면 기존처럼 최신 블로그 글을 보여 줍니다. 이미지를 폴더에 넣거나 네이버에 게시하는 것만으로 대시보드 내용이 자동 생성되지는 않습니다.

성경 본문은 번역본의 웹 게시 허가 범위를 확인한 뒤 화면에 싣습니다. 허가 범위가 확인되지 않은 경우에는 대한성서공회 새번역 본문으로 연결합니다.

## 검증된 연동 값 (변경 금지)

- 유튜브 채널 ID: `UC7OCid3xzRUptzmHUvA7B4g`
- 유튜브 RSS: `https://www.youtube.com/feeds/videos.xml?channel_id=UC7OCid3xzRUptzmHUvA7B4g`
- 블로그 RSS: `https://rss.blog.naver.com/wormwood79.xml`
- 최신 설교 표시 방식: 자동화 스크립트가 유튜브 RSS에서 최신 [주일예배] 영상을 찾아
  `data/latest-video.json`에 기록 → 홈페이지가 그 영상을 임베드.
  (업로드 재생목록 `videoseries?list=UU...` 임베드는 유튜브가 차단하는 경우가 있어 사용하지 않음)
